# Bloquer l'accès direct au dossier serial/
# Les scripts de lecture série doivent être exécutés
# exclusivement en ligne de commande (PHP CLI).

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
