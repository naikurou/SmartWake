# Bloquer l'accès direct au dossier includes/
# Ces fichiers sont inclus par PHP mais ne doivent
# jamais être accessibles directement via HTTP.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
